# Provenance: Shared Arena Frame options for 2–4 phones outdoors (L3 / KIL-20)

- **Date:** 2026-08-24 (all URLs accessed this date).
- **Rounds:** 1 prior-art pass over in-repo research (shared-ar-hit-registration.md, realtime-backend-options.md + backend-evidence/rbo-research-peer.md, roadmap L3, frozen requirements, CONTEXT.md glossary); 6 web research passes (ARWorldMap outdoors, collaborative sessions, Nearby Interaction/UWB + camera assistance, mutual-observation alignment literature, geo priors GPS/compass/GeoAnchor, shipped-product patterns + drift benchmarks); 1 derivation pass (error budget, §3 — repo-internal math, no external source); 1 synthesis.
- **Sources consulted:** ~45 fetched pages/papers across the passes (Apple developer documentation, Apple Support, WWDC 19/20/21/22 session pages, Apple Developer Forums, Stack Overflow practitioner threads, Unity AR Foundation docs and sample repos, Google ARCore docs and Creative Lab repos, Niantic Spatial docs and archived Niantic posts, Snap Engineering, ACM/IEEE/MDPI/arXiv papers, university theses, GIS practitioner reports).
- **Sources accepted:** 51 numbered sources in the brief. Apple primary documentation preferred; practitioner reports (Stack Overflow, Apple forums) used only where Apple publishes no number (world-map sizes, outdoor relocalization failures, real UWB range) and marked as such in the claim text.
- **Sources rejected / caveats:**
  - Apple's official SwiftShot sample page is offline; third-party README mirror used and marked (same handling as realtime-backend-options.md source 23).
  - `developer.apple.com/documentation/arkit/argeotrackingconfiguration` renders JS-only; the `.md` mirror endpoint was fetched instead (same canonical content). Some Apple doc pages were confirmed via the everest.mt Apple-docs mirror when the origin page failed to render; claims were kept only where wording matched Apple's own structure and the official URL is cited.
  - Apple `getCurrentWorldMap` objc variant page returned binary garbage on fetch; the Swift-path documentation and "Saving and loading world data" article used instead.
  - No official Apple source exists for: outdoor relocalization benchmarks, ARCollaborationData byte rates, NI concurrent-session cap (only the `activeSessionsLimitExceeded` error code), or a current GeoAnchor city list. These absences are stated in the brief as absences, not converted into claims.
  - SmartPoser arXiv ID reused from the in-repo peer memo (backend-evidence/rbo-research-peer.md source 31) rather than re-verified independently.
  - Victoria BC GeoAnchor coverage could not be verified from any public source; the brief mandates an on-device `checkAvailability` probe instead of assuming.
- **Speculation explicitly marked in the brief:** the §3.2 budget split (engineering proposal derived from frozen baselines, not literature); the §6.2 calibration ritual and its 60–120 s setup-time estimate; Option D (mutual visual observation) at iPhone implementation level; the §5.4 firing-unlock rule.
- **Verification:** self-review pass checked (a) every [n] resolves to the source list, (b) numeric claims match the cited source (0.02 m/s drift, 1.0–43.2 cm action drift, 25.1 cm walk-away, ~9 m UWB envelope, ~5 Hz NI rate, <20 cm UWB ranging error, 3.95/8.6 m GPS, 2–4°/13–25° compass, four-participant guidance, U1/U2 device lists, EDM iPhone 15+/iOS 17, Cloud Anchor quotas), (c) frozen repo baselines (0.35 m, 3–15 m, 250 ms, 100 ms) cited to the requirements doc rather than restated as external facts, (d) no duplication of the transport/authority conclusions already frozen in realtime-backend-options.md — that document is referenced, not re-argued.
- **Known limits:** all §3 consequences depend on the frozen 0.35 m / 3–15 m values; if KIL-18 review changes them the budget table must be re-derived. Every "Verdict" line is a recommendation pending KIL-20 physical-device measurements (§6.4 lists what cannot be known without them); this document deliberately does not propose an ADR before that evidence exists.
