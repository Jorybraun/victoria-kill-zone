# Build log

This is the integration-owned, evidence-based status record. Append observed results; do not paste secrets, signing material, unique device identifiers, or unsanitized logs.

## PR #70 review correction — 2026-09-07

Integration corrected the saved-arena management entry: only create-match mode can select an arena or render the lobby creation action. Management retains scanning and deletion; saving a scan does not select it for a match. The management heading and accessibility hint describe preparation rather than match creation.

Verification: `pnpm verify` passed (323 workspace tests and required gates); Xcode-toolchain `swift test --package-path ios/VictoriaKillZone` passed 329 tests with one physical-device-only skip and zero failures. This is local compile/test evidence, not physical-device acceptance or a deployment. Current-head GitHub iOS checks remain required before merge.

## Current status — 2026-08-22

- **Current gate:** Pipeline bootstrap, before G0 Hardware.
- **Observed working:** Canonical product sources, delivery ownership, CI/deploy workflows, and guarded Outpost operator scripts are present. At 2026-08-22 10:35 PDT, local `pnpm verify` passed (`Repository contract: PASS`; `Workspace verification: PASS`), all CI/Outpost shell scripts passed Bash syntax parsing, both workflow files passed YAML syntax parsing, and `pnpm verify:ios` correctly skipped because the iOS scaffold has not landed. Xcode 26.6 and Devin CLI 3000.5.20 are installed.
- **Physical-device evidence:** None recorded in this repository.
- **Mocked or unproven:** Signed launch on two phones; camera/location permission; per-phone Convex mutation/subscription; spectator subscription; dual mirroring; debug-fire vertical slice.
- **Blockers:** No canonical remote is configured and the stored GitHub CLI login for `Jorybraun` is invalid; Outpost credential rotation/restart is not evidenced; macOS reports zero valid code-signing identities and no two connected phones are evidenced; Convex/demo environment credentials are not evidenced; the automated CI result on the integrated bootstrap commit is not yet evidenced.
- **Next integration step:** Re-authenticate GitHub, publish the bootstrap commit to the canonical repository, obtain a green CI run, then run a read-only Mac Outpost canary against that exact SHA and execute G0.
- **Cut or deferred:** No Phase 0 feature cut. Unity/custom-server/LAN archives, 3+ player targeting, shared AR coordinates, UWB, projectile physics, WebRTC, permanent accounts, and App Store work are reference-only or post-MVP.

## Evidence required to close G0 Hardware

- [ ] Green `main` SHA recorded.
- [ ] Xcode version and build configuration recorded.
- [ ] Signed shell launches on Phone A (model/iOS only; no UDID).
- [ ] Signed shell launches on Phone B (model/iOS only; no UDID).
- [ ] Rear-camera permission passes on both phones.
- [ ] Precise foreground-location permission passes on both phones.
- [ ] Phone A mutation and live subscription pass against the configured Convex deployment.
- [ ] Phone B mutation and live subscription pass against the configured Convex deployment.
- [ ] Spectator shell live subscription passes against the same deployment.
- [ ] One tested mirroring route is recorded for each phone.
- [ ] Sanitized Outpost run link/log and time are recorded.

## Entry template

### YYYY-MM-DD HH:MM TZ — Gate or slice

- **Git SHA / PR:**
- **Owner and write set:**
- **Environment/artifact:**
- **Commands/checks:**
- **Observed on browser/simulator:**
- **Observed on physical devices:**
- **Mocked or unproven:**
- **Result:** PASS / FAIL / BLOCKED
- **Blocker and owner:**
- **Next integration step:**
- **Cut/deferred or risk change:**

### 2026-08-22 11:22 PDT — Six-hour G1/G2 checkpoint

- **Git SHA / PR:** Local `feat/g1-contracts-and-shells` worktree based on `a023465`; not yet integrated or promoted.
- **Owner and write set:** Integration (`docs/**`, root lock/verification), Design (`design/**`), iOS shell/client (`ios/**`), Spectator (`spectator/**`), Devin Cloud backend (`convex/**`).
- **Environment/artifact:** Xcode 26.6; root pnpm workspace; public canonical GitHub repository.
- **Commands/checks:** `pnpm install` completed and updated the workspace lockfile. `pnpm verify` passed at 11:12 PDT: repository contract, spectator lint, strict typecheck, 23 Vitest tests, and Vite production build all passed. The previously completed unsigned iOS shell build and six host-side XCTest cases passed; live-client changes are still in progress.
- **Observed on browser/simulator:** Spectator programmatic tests/build pass. No visual browser or simulator evidence recorded; no simulator runtime is currently evidenced.
- **Observed on physical devices:** macOS reports one usable Apple Development signing identity. Xcode reports two trusted physical iPhones. No signed install, launch, permission, mutation, subscription, or mirroring result is recorded yet.
- **Mocked or unproven:** Live iOS client, deployed Convex functions, live spectator subscription, signed two-phone run, idempotent debug fire, reconnect/recovery, and exact-SHA deployment.
- **Result:** BLOCKED at live backend/client integration; hardware discovery is now unblocked.
- **Blocker and owner:** Devin Cloud backend branch has not pushed an executable commit as of 11:22 PDT. Integration owns the 11:30 local-fallback decision. User owns keeping both phones connected, unlocked, and trusted.
- **Next integration step:** Receive or replace the backend implementation; reconcile both clients to `docs/interface-contracts.md`; run exact-head verification and open the first green PR.
- **Cut/deferred or risk change:** The 17:00 demo scope is G2 only. Vision/ARKit targeting, geofence enforcement, radar, K/D, kill/respawn, reload, sound, haptics, and presentation polish are deferred.

### 2026-08-25 10:40 PDT — KIL-38 TestFlight promotion automation (Tier 0/1 lane logic only)

- **Git SHA / PR:** branch `kil-38-testflight-promotion-lane` based on `40d56f38b3db19d0d84ad1f2e1edb4bf269c2316`.
- **Owner and write set:** Integration (`.github/workflows/**`, `scripts/release/**`, `docs/outpost-operations.md`, this log).
- **Environment/artifact:** ephemeral Mac Outpost VM, macOS 26.6.2 (25G83), Xcode 26.4.1 (17E202), Node 26, pnpm 10.11.0.
- **Commands/checks:** `node scripts/release/testflight-self-test.mjs` PASS (gate stale-SHA skip, disabled mode, non-push/non-CI trigger rejection, exact-build-number correlation, successful processing, timeout, upload failure, Slack failure, redaction). `bash scripts/release/self-test.sh` PASS. `bash -n scripts/release/testflight-upload.sh` PASS. `pnpm verify` PASS (`Repository contract: PASS`; `Workspace verification: PASS`) with the session-injected `VITE_CONVEX_URL`/`CONVEX_DEPLOYMENT_URL` unset; those inherited variables otherwise divert spectator tests off their demo fixtures.
- **Observed on browser/simulator:** none for this slice; no archive, signing, or upload was executed.
- **Observed on physical devices:** none. No OTA install is claimed.
- **Mocked or unproven:** every App Store Connect and Slack interaction is fixture-driven. The self-hosted Outpost runner, Distribution signing identity, `.p8` key, and repository secrets/variables do not exist yet; this VM reports zero valid code-signing identities and no App Store Connect key directory.
- **Result:** PASS for lane logic; BLOCKED for end-to-end promotion.
- **Blocker and owner:** Hans owns the one-time setup — register the `self-hosted, macos, vkz-outpost` runner on the persistent Outpost, unlock its keychain with a Distribution identity, install the Admin-role `.p8`, and set `VKZ_ASC_KEY_ID`, `VKZ_ASC_ISSUER_ID`, `VKZ_SLACK_WEBHOOK_URL`, plus `VKZ_TESTFLIGHT_ENABLED`, `VKZ_MARKETING_VERSION`, `VKZ_BUNDLE_ID`, `VKZ_SLACK_CHANNEL_ID`.
- **Next integration step:** merge the lane disabled, complete the one-time setup, then flip `VKZ_TESTFLIGHT_ENABLED` and observe one promotion end to end.
- **Cut/deferred or risk change:** Tier 2 for KIL-38 stays open until OTA installation is observed on both phones.

### 2026-08-25 11:05 PDT — KIL-38 correction round 1 (lane hardening, still Tier 0/1)

- **Git SHA / PR:** branch `kil-38-testflight-promotion-lane`, second commit; PR #31.
- **Owner and write set:** Integration (`.github/workflows/**`, `scripts/release/**`, `docs/outpost-operations.md`, this log).
- **Commands/checks:** `node scripts/release/testflight-self-test.mjs` PASS, `bash scripts/release/self-test.sh` PASS (`Release shell self-tests: PASS`), `bash -n scripts/release/testflight-upload.sh` PASS, `pnpm verify` PASS (`Workspace verification: PASS`) with `VITE_CONVEX_URL`/`CONVEX_DEPLOYMENT_URL` unset.
- **Corrections:** manual dispatch can no longer bypass CI (gate reads current `main` and the revision's successful `CI` push run from the API and fails closed on lookup failure); the Mac job revalidates current `main` immediately before archiving and reports `skipped-stale` if it moved; App Store Connect requests use a refreshing token provider instead of one 15-minute JWT; build output is redacted before reaching the workflow log or any caller; archive facts must match the promoted revision and their marketing version supersedes the configured one; unpersisted evidence makes the result non-success without rewriting the original failure.
- **Observed on physical devices:** none. No OTA install and no live TestFlight update are claimed.
- **Mocked or unproven:** all App Store Connect, GitHub API, and Slack interactions remain fixture-driven; the self-hosted runner, signing identity, and `.p8` still do not exist.
- **Result:** PASS for lane logic; still BLOCKED for end-to-end promotion. Lane remains disabled by default (`VKZ_TESTFLIGHT_ENABLED` unset).
- **Blocker and owner:** Hans — one-time Outpost runner, keychain identity, `.p8`, and repository secrets/variables.
- **Next integration step:** review PR #31, merge disabled, complete one-time setup, then enable and observe one promotion.

### 2026-08-25 11:25 PDT — KIL-38 correction round 2 (permissions, CI identity, evidence-before-status)

- **Git SHA / PR:** branch `kil-38-testflight-promotion-lane`, third commit; PR #31.
- **Owner and write set:** Integration (`.github/workflows/**`, `scripts/release/**`, `docs/outpost-operations.md`, this log).
- **Commands/checks:** `node scripts/release/testflight-self-test.mjs` PASS, `bash scripts/release/self-test.sh` PASS, `bash -n scripts/release/testflight-upload.sh` PASS, `pnpm verify` PASS (`Workspace verification: PASS`) with `VITE_CONVEX_URL`/`CONVEX_DEPLOYMENT_URL` unset.
- **Corrections:** the workflow declares `actions: read` for the workflow-runs lookup, and the CI proof is scoped to the canonical `.github/workflows/ci.yml` workflow file rather than any workflow whose display name is `CI` (a 403 or any other lookup error fails closed); sanitized evidence is now persisted before the terminal status is posted, so an unrecordable promotion posts a `failed` status instead of leaving `#pew-pew-releases` claiming a release, while the returned and recorded state still preserves the App Store Connect outcome.
- **Observed on physical devices:** none. No OTA install and no live TestFlight update are claimed.
- **Mocked or unproven:** all App Store Connect, GitHub API, and Slack interactions remain fixture-driven; the self-hosted runner, signing identity, and `.p8` still do not exist.
- **Result:** PASS for lane logic; still BLOCKED for end-to-end promotion. Lane remains disabled by default.
- **Blocker and owner:** Hans — one-time Outpost runner, keychain identity, `.p8`, and repository secrets/variables.
- **Next integration step:** review PR #31, merge disabled, complete one-time setup, then enable and observe one promotion.

### 2026-09-02 18:20 PDT — KIL-20 shared-arena harness (Tier 0 + compile only; physical run pending)

- **Git SHA / PR:** branch `kil-20-shared-arena-harness` (`46d9ad7`, `b78b44f`) on top of `main` `332efe0`; draft PR to follow.
- **Owner and write set:** iOS targeting (`ios/VictoriaKillZone/VictoriaKillZone/Targeting/SharedArena/**`, `VictoriaKillZoneTests/SharedArenaFrameTests.swift`). Integration handoff items in the same PR: `Package.swift` sources list, `project.pbxproj` file registration, `Info.plist` (`NSLocalNetworkUsageDescription`, `NSBonjourServices`), one `HomeView` navigation link to the harness.
- **Environment/artifact:** Xcode 26.6 (17F113), macOS host; no phones attached to this session.
- **Commands/checks:** `pnpm verify` PASS (`Repository contract: PASS`, `Workspace verification: PASS`); `pnpm verify:ios` PASS (92 SwiftPM tests incl. 13 new `SharedArenaFrameTests`; unsigned simulator builds of `VictoriaKillZone` and `VictoriaKillZoneTests`).
- **What landed:** fail-closed `SharedArenaLockPolicy` (clears peer transform history on loss; re-lock requires 10 consecutive clean evaluations; 0.10 m / 0.5° residual gate from research §3.2 as configurable thresholds), `ArenaPeerSample` codec, `ArenaFrameMetrics` (interval p50/p95/p99, loss, out-of-order, pose age, recovery time), §6.3 CSV log, Bonjour/Network.framework harness link, `SharedArenaSession` running either `collaborative` (primary; `ARParticipantAnchor` residual) or `worldMap` (control) behind one surface, harness screen rendering three named anchors and the 0.35 m peer proxy only while `lockReady`.
- **Observed on browser/simulator:** compile only. No simulator run claimed (ARKit world tracking and Bonjour peer discovery require devices).
- **Observed on physical devices:** none.
- **Mocked or unproven:** collaborative merge outdoors, world-map relocalization, alignment error at 3/8/15 m, interruption/re-lock recovery, update-interval percentiles, bandwidth, thermal — all of KIL-20's physical acceptance. The harness link is scoped to the proof; it is not the KIL-35 `CombatTransport` (512-byte reliable payload cap cannot carry map/collaboration blobs).
- **Result:** PASS for Tier 0 and compile; BLOCKED for KIL-20 acceptance until the two-phone run.
- **Blocker and owner:** Hans — two signed iPhones (record models + iOS), park site with tape measure, run the §7 protocol from `docs/research/shared-arena-frame-options.md` in both methods, export both CSVs.
- **Next integration step:** review/merge the draft PR, install via TestFlight or cable, run the protocol, record device evidence here; then decide whether the §3.2 thresholds hold or need re-cutting before KIL-22 freezes `spatial-hit.v1`.
- **Cut/deferred or risk change:** chunked bulk channel in `CombatTransport` deferred to KIL-35 follow-up; NI/UWB cross-check (research Option C) not built in this slice.

### 2026-09-03 08:10 PDT — KIL-38 TestFlight lane live end to end (Tier 2 partially proven)

- **Git SHA / PR:** `74beac1` (run 33770530270, build 13) and `4e370b5` (run 33778613983, build 14, PR #41). Runner setup performed on the persistent Outpost Mac; no source change for the runner itself.
- **Owner and write set:** Integration (`docs/outpost-operations.md`, this log; PR #41 touched two `MARKETING_VERSION` lines in `project.pbxproj`).
- **Environment/artifact:** persistent Mac Outpost (`vkz-outpost`, macOS 26.5, Xcode 26.6 17F113), self-hosted GitHub Actions runner 2.337.0 as a user LaunchAgent. Repository secrets `VKZ_ASC_KEY_ID`, `VKZ_ASC_ISSUER_ID`; variables `VKZ_TESTFLIGHT_ENABLED=true`, `VKZ_BUNDLE_ID`, `VKZ_MARKETING_VERSION=0.1.1`. Slack not configured (lane treats it as an observer).
- **Commands/checks:** run 33770530270 (manual `workflow_dispatch` on `main`): gate PASS, archive/sign/upload PASS, App Store Connect `processingState=VALID`, evidence `ready-for-testing` 0.1.0 (13). Run 33778613983 (self-triggered `workflow_run` after merging #41): gate PASS, promoted 0.1.1 (14), Apple processing email received by the operator. App Store Connect API (read-only) confirms both builds `IN_BETA_TESTING` in group "Pew Pew Internal" (auto-distribute on, 2 testers, both `INSTALLED`).
- **Corrections found on the way:** (1) the runner's active developer directory was Command Line Tools → `DEVELOPER_DIR` set in the runner `.env`; (2) `CodeSign` failed with `errSecInternalComponent` because the generated LaunchAgent plist had `SessionCreate=true`, isolating the runner from the unlocked login keychain → set to `false`; (3) build 13 sorted under the 2026-08-25 manual upload (build 20260825) on the same 0.1.0 train → opened the 0.1.1 train (#41). No local Apple Distribution identity is needed: the lane uses cloud signing through the Admin-role key.
- **Observed on physical devices:** operator received Apple's processing email for 0.1.1 (14). OTA install on a named device/iOS version not yet recorded; `physicalDeviceEvidence` remains `not-claimed` in both evidence artifacts.
- **Mocked or unproven:** none in the lane; the pre-existing `LobbyStoreTests.testDebugFireAfterConfirmationUsesNewClientShotID` flaked once on an unrelated PR (#38) and passed on rerun of the same SHA — timing race, not addressed.
- **Result:** PASS — every green merge to `main` now promotes to TestFlight automatically. Tier 2 closes when an OTA install is observed on both phones and recorded here.
- **Blocker and owner:** none for the lane. The Outpost Mac must stay logged in and awake; a queued promote job is the intended fail-closed state when it is not.
- **Next integration step:** record the two OTA installs (model + iOS version only); optionally add `VKZ_SLACK_WEBHOOK_URL`/`VKZ_SLACK_CHANNEL_ID`.

### 2026-09-04 — ADR 0005 proposed

- ADR 0005 proposed; fire-path trace found every markerless `shots:fire` rejected `LOCATION_STALE` on the current client (no location sent); PR implements ADR 0005; physical-device evidence pending.
- Follow-up hardening PR (match-scoped peer link, skeleton scale estimation, pure hit-geometry tests)
### 2026-09-04 11:30 UTC — ADR 0004 §3 backend slice: shots:recordVerdict durable ledger (Tier 0)

- **Git SHA / PR:** branch `devin/1788521140-record-verdict`; PR #49.
- **Owner and write set:** Backend (`convex/**`); Integration handoff items in the same PR: `docs/interface-contracts.md` (verdict-ledger.v1 section), this log.
- **Commands/checks:** `pnpm verify` PASS (backend lint/typecheck/tests incl. new `record-verdict.test.ts`; spectator untouched and green).
- **What landed:** `shots:recordVerdict` host-only mutation, idempotent per (matchId, clientShotId); `resolveFire`/`resolveDebugFire` and the verdict path share one `applyVerdict`; additive `shots`/`events` schema fields and `by_match_and_client_shot_id` index; `targetConfirmed` surfaced on snapshot events. `shots:fire` and `shots:debugFire` behaviour unchanged (existing fire tests untouched and passing).
- **Observed on physical devices:** none. No client calls the new mutation yet.
- **Mocked or unproven:** host-to-Convex round trip (ADR 0004 p95 ≤ 500 ms), receiver confirmation flow, live schema push against the deployment (runtime validator not exercisable offline; compatibility asserted at type level).
- **Result:** PASS for Tier 0; BLOCKED for ADR 0004 acceptance until the iOS host authority posts verdicts and the two-phone TestFlight run records fire→confirmation latency.
- **Next integration step:** iOS slice wires `AuthorityHost` verdicts to `shots:recordVerdict`; then measure on two phones.

### 2026-09-04 — App Store readiness audit (Info.plist, privacy manifest, Release gating of debug UI)

- **Git SHA / PR:** Branch `devin/1788521289-app-store-readiness`; PR pending.
- **Owner and write set:** Integration (Xcode project, Info.plist, docs) + iOS targeting (Targeting/SharedArena gating).
- **Environment/artifact:** Linux repository workspace; iOS source and Xcode project metadata.
- **Commands/checks:** `pnpm verify` PASS (`Repository contract: PASS`; workspace lint, typecheck, tests, build, and `Workspace verification: PASS`).
- **Observed on browser/simulator:** None.
- **Observed on physical devices:** None. Needed two-phone TestFlight checks: camera-denied panel; harness link absent in the TestFlight build; Bonjour prompt still works for both `_pewpew-arena._tcp` and `_vkz-combat._udp`.
- **Mocked or unproven:** Physical-device camera-denied flow, Release/TestFlight UI, and Bonjour prompts.
- **Result:** BLOCKED pending physical-device TestFlight checks.
- **Blocker and owner:** Integration / iOS targeting — two-phone TestFlight validation.
- **Next integration step:** Install the pushed build on two phones and run the listed TestFlight checks.
- **Cut/deferred or risk change:** Release excludes debug-only harness and shell controls; debug-fire paths remain available. The torso-fallback fire button is gated by `VKZ_DEBUG_FIRE`, set in both Debug and Release so TestFlight keeps it; remove it from Release only after two-phone markerless fire evidence lands here.

### 2026-09-04 — ADR 0006 duel shared frame proposed (docs only)

- **Git SHA / PR:** this PR (docs-only; no product code, no tests changed).
- **Owner and write set:** Integration — `docs/decisions/0006-duel-shared-frame.md` (new), `docs/decisions/0005-…` (one-line correction), `docs/roadmap.md`, this log.
- **Environment/artifact:** Apple ARKit/Vision documentation and WWDC19/23 transcripts cited inline in the ADR; prior research `docs/research/shared-arena-frame-options.md`.
- **Commands/checks:** `pnpm verify`; `ios-gate` CI (no Swift touched).
- **Observed on browser/simulator:** none.
- **Observed on physical devices:** none — nothing in this slice is device evidence.
- **Mocked or unproven:** every ADR 0006 §7 row: body-tracking relocalization into a peer's `ARWorldMap` outdoors, residual at 3/8/15 m, drift over a 3-minute duel, map transfer time over `CombatTransport`, receiver-confirmation false-hit reduction, thermal. Drift numbers in the ADR are published planning values, not measurements of this app.
- **Result:** PASS (documentation slice). ADR 0006 status stays Proposed.
- **Blocker and owner:** two body-tracking-capable iPhones + operator time for the S0 spike (Hardware/Operator).
- **Next integration step:** dispatch S0 (iOS targeting) per ADR 0006 §9; record §7 rows 1–3 here with model + iOS version only.
- **Cut/deferred or risk change:** roadmap ADR index renumbered — personal-time semantics → 0007, street-scale spatial provider → 0008. Collaborative ARKit sessions are no longer the duel's primary frame; retirement list in ADR 0006 §8 executes only in its named slices, never before S0 evidence.

### 2026-09-04 — DuelSession combat split

- **Owner and write set:** Integration+iOS (`ios/VictoriaKillZone/VictoriaKillZone/Features/Game/**`, `Features/Lobby/LobbyStore.swift`, `ActiveDuelView.swift`, `RootView.swift`, iOS tests, Xcode project registration, this log).
- **What moved:** Duel combat state, fire actions, pending-shot reconciliation, kill/incoming event presentation, peer tracer handling, and shared game-loop tracing moved from `LobbyStore` into `@MainActor DuelSession`; combat views now observe the dedicated session.
- **What hardened:** Peer tracers are accepted only from the current opponent, and Convex incoming-event deduplication is bounded to 256 IDs with ordered eviction.
- **Observed on physical devices:** none.
- **Two-phone TestFlight confirmation required:** peer tracer renders on the other phone; Convex miss suppression works while hit/eliminated events still render; peer link starts and stops across phase changes; debug fire remains unchanged.
# 2026-09-04 — Combat feedback, cadence and architecture review (slice 004)

- Branch: `codex/combat-quality-review`, based on release-fix `a5dbc73` / PR #53. Review order: #53 first, then the combat draft. No direct main push, merge, deployment or signing attempted.
- Ownership: integration owns docs/design tuning, native input/HUD/home/models/client, simulation tuning and Xcode registration. Rendering owns LaserFX and presentation policy/tests. Backend owns convex/**. Existing `.hermes/` and `skills-lock.json` user files remain untouched and excluded.
- Implemented: hit-only 280 ms skeleton; separate outgoing hit and incoming damage feedback; bounded pooled tracers/impacts; cosmetic muzzle parallax; camera-centered reticle; redesigned home/combat HUD; 150 ms sidearm cadence and hold-to-fire; explicit authoritative reload; foreground heartbeat; exact replay for uncertain shots; ordered incoming batches and per-shot peer/durable deduplication. Production input no longer falls back to debug damage. Debug control remains gated.
- Backend: direct target reads instead of whole-match player reads; shooter-scoped idempotency; authenticated adjudicator binding; immutable retries after completion; finite evidence checks; reload/death/scheduler guards; optional reload deadline and shot identity projections. Convex remains the backend.
- Automated evidence on this working tree: `pnpm verify` PASS (108 backend + 30 spectator tests, lint/typecheck/build/repo checks); native SwiftPM suite PASS (148 tests); deterministic simulation suite PASS (74 tests). Total 360 executed tests, zero failures. SwiftPM used Xcode's developer directory, temporary module caches and `--disable-sandbox` because nested sandbox/default-cache writes were unavailable.
- Unsigned native app compile: final Debug and Release app builds and the Debug iOS test target build PASS with the simulator SDK, arm64, no signing, no simulator launch required. This is compilation evidence only.
- Simulator scene execution: NOT VERIFIED. Attempting the iPhone 17 Pro simulator scene run exhausted host disk space; Xcode exited 75 before producing execution evidence. Only this task's temporary generated build/index/test artifacts were removed to recover space. No simulator screenshots or rendering performance measurements are claimed.
- Physical evidence: NONE. Shared-frame alignment, correct player/body association, outdoor body tracking, haptics, peer authenticity/latency, sustained firing/thermal behavior and two/four-phone convergence remain unproven. The game is not declared production-ready by this entry.
- Architecture: [review](research/production-combat-review.md) recommends shared-frame proof and one connected combat authority before a vendor migration. Real projectile collision, slowdown fields, oriented phone shields and Durable Objects are specified/reviewed, not shipped. [ADR 0007](decisions/0007-combat-feedback-and-cadence.md) accepts only presentation/tuning changes; prior physical gates remain intact.
- Next owner/evidence: integration + device operator, two named iPhone models/iOS versions for ADR 0006 calibration/trajectory trials, followed by authority integration and the four-phone gate. Free host disk capacity before retrying simulator scene execution.

# 2026-09-05 — Full review implementation, realtime engine and durable authority

- User authorized all M0–M6 work and created the active full-scope goal. ADR 0008 records implementation authority; branch `codex/realtime-combat-foundation` follows draft PR #55 and release-fix #53. No merge, live deployment or signing is claimed.
- Protocol: strict bounded command/ticket/projection validation; authenticated identities, stable collider IDs, accepted remote phone poses, synchronized logical clock and separate round start; durable command results and ordered replay. Native socket, clock, atomic replica, bounded retry session and authenticated 8 MiB map client implemented.
- Convex: four-slot realtime lobby, host-frozen roster, 120-second signed capabilities, one-combat-writer enforcement across public and scheduled legacy paths, HMAC-authenticated ordered projections, complete terminal ledger and spectator score projection. Legacy matches retain their debug path. Authentication, replay, gap/conflict, roster and writer-isolation tests pass.
- Pure engine: continuous swept sphere/moving-capsule collision, finite projectiles, hitscan, front-face shields, continuous min-overlap slow fields, reload/respawn/protection, fail-closed tracking and deterministic recovery. Compact checkpoints omit discarded-on-recovery tracking histories; a worst fixture shrank from over 500 KiB to under 16 KiB while retaining gameplay state. This is fixture size evidence, not a live cloud cost measurement.
- Worker: one SQLite Durable Object per match; staged simulation commits before broadcast; bounded input/output history, actual WebSocket authentication/recovery tests, immutable authenticated chunked maps, independent bullet ledger and durable FIFO projection outbox. Review caught and corrected tick-anchor drift, replacement-socket readiness and receipt byte accounting.
- Canonical evidence: `pnpm verify` PASS on this implementation checkpoint, including repository/lint/typecheck/tests/build and Wrangler deployment dry run. Log: `/tmp/vkz-realtime-verify.log`. Subsequent native composition work must repeat the relevant checks before review.
- Native evidence before the next UI/FX integration edits: 175 SwiftPM tests PASS; new calibration provider has same-session ARKit adapter and secure archive/epoch/timeout/residual gates. Real iOS SDK typechecks for calibration/anatomy passed separately. Full app compile after all new Xcode entries remains pending.
- Visual evidence: [actual procedural skeleton renders](../design/evidence/005-shared-combat/README.md), using synthetic landmark input, inspected and refined. These establish recognizable 3D anatomy only. Hit-only visibility is retained; physical alignment, occlusion, camera observations and sustained performance remain unproven.
- In progress: full realtime arena controller/UI and worldline/shield/field rendering, root lobby/route wiring, independent common-scene residual measurement and physical 2–4 player evidence, host/DO comparison and release gates. The goal remains active. No partial subsystem is represented as completing the whole report.

# 2026-09-05 — Arena integration and dedicated skeleton design team

- The owner rejected the first procedural skeleton's visual quality. Slice 006 supersedes its art acceptance: dedicated art direction, anatomy/modeling, and independent visual review. Actual optimized BodyParts3D source views now establish a substantially better anatomical base; skull seams/teeth, final materials, retargeting and gameplay-scale reveal evidence remain open. A render is not a physical tracking or performance result.
- Integration owns lobby/routing, native package/Xcode registration, credits, clock/session and integration tests. Model ownership is limited to `Features/Game/Skeleton*`, anatomy tests and slice 006 model evidence; art direction owns the written slice. Shared path handoffs remain explicit.
- Native arena controller/view now connects the prepared match, transfers the map, displays calibration/reconnect/respawn/results, and exposes fire/reload/shield/slow-field controls. Four-player lobby snapshots route to one combat controller; classic duel remains available for existing validation. Leaving awaits actual camera teardown before another session can start; overlapping start/stop tests cover the camera races.
- Identity conservatism: missing/stale competing phones do not create a false nearest-phone margin, and eliminated bodies remain identity competitors. A confirmed hit only flashes the currently associated target. Target brackets use the actual AR scene viewport and upper-left coordinates, fixing the old square-viewport/Y-flip mismatch.
- Clock discontinuity/expiry closes the authority connection rather than timestamping a false readiness claim with an uncertain clock. The authority then clears readiness; fresh clock synchronization is required before input resumes.
- `pnpm verify` PASS: 12 protocol + 39 simulation + 118 Convex + 38 actual workerd + 30 spectator tests, plus lint/typecheck/build/repository checks and Worker dry run. Log: `/tmp/vkz-arena-workspace-verify.log`. The first sandboxed run could not bind the local test socket; the authorized run with loopback networking passed. No live deployment occurred.
- Integrated unsigned iOS Debug app build and Debug `build-for-testing` PASS before final anatomical asset changes; logs `/tmp/vkz-arena-xcode-debug.log` and `/tmp/vkz-arena-xcode-tests.log`. Corrected 11 unresolved Xcode source paths and registered missing native sources/tests. Native SwiftPM suite: 211 tests PASS, including clock discontinuity, camera start/stop races, four-player routing, stale-competing-phone identity and portrait target projection; log `/tmp/vkz-arena-integration-final-swift.log`. The package now discovers Swift sources separately from its copied asset folder, avoiding duplicate source/resource rules. Final asset builds remain required; iOS scene tests were compiled, not executed.
- The BodyParts3D official archive publishes CC BY 4.0 terms updated 2025-02-27. Provenance/license evidence ships beside the derived mesh and the app provides a Credits screen with attribution, source/license links and adaptation notice. The original download stays outside the app/repository.
- No physical device, independent residual collector, latency, GPU, thermal, signed installation or full M0–M6 completion is claimed. The full implementation goal remains active.

# 2026-09-05 — Game-first integration after restart

- Owner steering: integrate the anatomical asset, stop extended skeleton polish, finish the native game before further server work. Integration recovered the existing branch and draft PR #56; its review dependency remains #53 → #55 → #56. No main push, merge, live deployment or signing occurred in this checkpoint.
- The app now bundles the actual 19-part anatomical mesh (51,796 triangles; 1,695,385 bytes), source/license manifest and Credits screen. The durable asset builder passes six checks and rebuilds byte-identically from its pinned cached source. The 64 MB source archive, virtual environment and working caches are ignored. The shared production hit-reveal controller is hidden ordinarily, holds through 100 ms and expires at 280 ms; repeat hits restart its window and stale targets clear it. [Native render evidence](../design/evidence/006-skeleton-model/README.md) is synthetic SceneKit/Metal evidence, with skull simplification/rig refinements still open.
- Setup now has a concrete natural-scene reference capture, bounded authenticated calibration bundle, reference thumbnail, recoverable capture errors and fresh tracked-image residuals. Sensor backlog retains its real age; cached anchors/repeated timestamps cannot renew permission. A legacy map cannot open readiness and the UI explains how to create a new arena. [ADR 0009](decisions/0009-natural-scene-calibration-candidate.md) corrects the prior head-to-phone/saved-anchor assumption. This candidate still requires the reference to remain visible; no unrestricted movement or physical accuracy is claimed.
- Gameplay fixes: pending start/reload/shield/field state; sanitized rejected-action feedback; shot-correlated ammo reservations across split event batches and reconnect snapshots; bounded readiness retries until authority state agrees; background suspension that closes the socket and retains exact unconfirmed command identities. Fatal admission errors stop retry loops, transient failures expose actionable retry, and stale callbacks cannot close replacement connections. Realtime leave performs exactly one awaited camera teardown before a new arena can start.
- Home and lobby now distinguish arenas from classic duels, display actual player capacity/duration, compact invitation controls, explain readiness and keep primary actions reachable on small screens. The combat HUD uses friendly weapon names and accepted shield/field/protection/reload timing. [Ten actual SwiftUI renders](../design/evidence/007-gameplay-ui/README.md) verify synthetic desktop layout and scrolling; macOS text metrics, clipboard/QR, touch, keyboard and VoiceOver are not iPhone evidence.
- Final automated evidence on the integration working tree: `pnpm verify` PASS (237 tests: 12 protocol, 39 simulation, 118 Convex, 38 workerd, 30 spectator; lint/typecheck/build/repository checks and Worker dry run); full native SwiftPM PASS (258 tests, zero failures, including 23 frame/reference and 15 connection/suspension checks). Logs: `/tmp/vkz-game-final-workspace.log`, `/tmp/vkz-game-final-swift.log`.
- Final unsigned native Debug app/test build and Release app build PASS with arm64 iOS Simulator SDK. Logs: `/tmp/vkz-game-final-xcode-debug.log`, `/tmp/vkz-game-final-xcode-release.log`. Corrected a duplicate framework build ID and registered all sources/resources. The new CI source-membership check passes for 59 app and 24 test files, preventing tests or app files from silently disappearing from the Xcode target. Simulator test bundles were compiled, not executed; SwiftPM tests ran on macOS.
- Remaining acceptance: named physical iPhones/iOS versions for same-frame alignment plus body coverage, two/four-player identities and convergence, actual rapid-fire/hit/shield/slow-field input and feedback, stationary-reference visibility during movement, target-space uncertainty, sustained thermal/GPU/battery/frame-time, permissions/VoiceOver, reviewed green-main promotion and signed installation. The full M0–M6 goal remains active. Further backend work is queued after the game integration pass; no new production-server performance claim is made here.
### 2026-09-04 — CombatTransport becomes the peer plane (loopback + compile gate only; two-phone run pending)

- **Change:** `CombatTransportArenaLink` (QUIC/Bonjour host-relay via the KIL-35 package) becomes the primary duel fast path and Shared Arena harness link, wrapped in `FallbackArenaPeerLink`, which falls back to the KIL-20 TCP `ArenaPeerLink` (kept, match-scoped by Bonjour instance name, re-framed over `ArenaLinkBodyCodec`) when QUIC does not connect within 3 s; `ArenaShotTracerCodec` and the old body encoding deleted; `NSBonjourServices` lists `_vkz-combat._udp` and `_pewpew-arena._tcp`. Match scoping (`MatchScope`: hashed scope id in Bonjour name + TXT record, PSK from match id + join code, `MatchHello` first frame), `CombatFireMessage` (`shot` with monotonic `firedAtMs`, `retracted(shotId)`), and `NetworkPeerLinkEvent` added to the package. See ADR 0004 "Transport integration".
- **Follow-up:** TCP fallback now performs mutual PSK challenge-response before accepting `ArenaLinkMessage` bodies.
- **Commands/checks:** `pnpm verify` PASS locally; macOS `ios-gate` (`swift test` on both packages + xcodebuild) is the Swift gate. Loopback tests cover host/guest shot, retraction, 200 KiB world-map bulk transfer, and wrong-match rejection (TXT filter, PSK slot claim, hello scope).
- **Observed on physical devices:** none. Not claimed.
- **Mocked or unproven:** QUIC TLS identity — the app passes no `TransportIdentityProvider`, so the on-device QUIC handshake is expected to fail and the TCP fallback to carry the duel until an ephemeral per-match identity is added; PSK-only QUIC is not offered by Network.framework (ADR 0004 deviation 3, Apple DTS citation). Which path went live on device is unrecorded. Poses still ride the reliable channel, not datagrams.
- **Handoff:** DuelSession owner (post-#51 `LobbyStore` split; `DuelSession.defaultPeerLink` now builds the QUIC+TCP fallback link) — send `.shotRetracted(shotId:)` on the duel link when `shots:fire` rejects a shot; the `updateDuelPeerLink` construction line is the only LobbyStore change in this PR. PR #46 also touches `ArenaPeerLink.swift`/that line and will conflict; resolve by keeping `CombatTransportArenaLink`.
- **Next integration step (two-phone TestFlight run):** ephemeral identity + public-key pinning, then measure fire → peer tracer p95 ≤ 50 ms, fire → Convex confirmation p95 ≤ 500 ms, pose p99 age ≤ 100 ms once poses move to datagrams, and wrong-match rejection with a third phone.

### 2026-09-05 — PR #55 parent conflict resolution

- Integration merged parent PR #53 at `44f794c` (including main's #52 transport and #54 UI fixes) into `codex/combat-quality-review` in an isolated checkout. Conflict write set: this log, Xcode project, ActiveDuelView and LaserFX; the related DuelSession debug-retry gate and its tests were explicitly handed to the native owner and reviewed by integration. PR #56 and its unfinished server benchmarks remain separate.
- Preserved 150 ms hold-to-fire, authoritative reload, hit-only 280 ms skeleton feedback, captured camera-ray tracers and bounded effect pools. Retained inline shot notices, Local Network guidance and the match-scoped QUIC/TCP transport. Incoming bolts retain the off-centre endpoint presentation without fabricating a target or revealing unhit skeletons. Resolved the duplicate Xcode build-file ID shared by simulation linkage and asset resources.
- Semantic regression fixed: a lost response to the final debug round remains retryable with its exact retained shot ID when ammo reaches zero. A resolved or authoritatively rejected request cannot start a new empty-magazine shot; two new native regressions cover both paths.
- Local evidence on the resolved tree: `pnpm verify` PASS (108 Convex + 30 spectator tests); native SwiftPM 168 tests PASS; simulation 74 tests PASS; transport 79 tests PASS. Total 459 tests, zero failures. Unsigned Debug app and iOS test bundle compile PASS; unsigned Release app compile PASS. No physical-device, signing, deployment or on-device QUIC claim is made.
- Independent review found existing parent transport debt: CombatTransportArenaLink's queued callbacks do not carry a start generation, and its reliable-event orderer is retained on restart. This is unchanged incoming code, tracked for the subsequent gameplay integration rather than represented as verified restart safety here. Review/merge order remains #53 → #55 → #56.

### 2026-09-05 — PR #55 propagation into the full game (PR #56)

- Integration carried the resolved parent into the existing game branch, preserving the anatomical asset, hit-reveal controller, natural-reference calibration and four-player UI. Conflict ownership: integration log/Xcode/routing, design WaitingRoomView, targeting transport lifecycle/tests; all handoffs were explicit. The paused server benchmark files remain uncommitted and outside this review update.
- Kept legacy inline shot errors without suppressing realtime arena errors; Local Network permission guidance is scoped to classic peer matches. Corrected the second Xcode ID collision (transport linkage versus body geometry); source membership now verifies 63 app and 27 test files.
- Incoming transport callbacks now carry a session generation and endpoint identity, failures invalidate old callbacks, and fresh sessions reset reliable ordering. Two deterministic regressions prove fresh sequence-one acceptance and rejection of saved callbacks even when the same endpoint object is reused.
- PR #56's prior CI-only projectile failure was an acknowledged refusal, not a missing event: the fixture reused expired admission timestamps and assumed cross-socket send order. The separate test repair synchronizes a current authority clock, waits for accepted readiness/pose/start/fire results, refreshes poses at 20 Hz, preserves both-client projectile comparison and asserts the exact persisted spawn ledger. Runtime authority limits remain unchanged; all 38 worker tests and five focused repeats passed.
- Final native tests: 280 PASS; shared simulation 74 PASS; transport 79 PASS. Unsigned Debug app/test bundle and Release app builds PASS. Final `pnpm verify` PASS (237 workspace tests; lint/typecheck/build/repository checks), for 670 automated tests across the verified packages. These are automated/compile results only; all prior physical-device, signing, deployment, thermal, latency and M0–M6 acceptance gates remain open.
### 2026-09-05 — PR #55 CI transport fixture repair

- The first conflict-resolved head was mergeable, but its duplicate GitHub runs disagreed: one iOS gate passed and the other timed out on `testShotRetractionTravels`. Investigation found a test fixture race: it advanced the synchronous LoopbackFabric before both asynchronous adapters installed receive handlers, and concurrently accessed the simulator event array. A positive byte count only proved a hello had been sent, not the tested gameplay frame.
- Integration accepted a test-only serialized simulator wrapper, a two-hello setup barrier, explicit connected prerequisites and byte-count deltas for gameplay sends. Existing test timeouts and production transport behavior remain unchanged.
- Verification after the fixture repair: `pnpm verify` PASS, all 168 native tests PASS, the six adapter tests PASS in five additional consecutive runs, and unsigned Debug app/test-bundle compile PASS. The prior unchanged Release app build remains applicable. GitHub checks rerun on the repaired head; no red/flaky check is waived.

### 2026-09-05 — Shared game CI fixtures use explicit time and delivery barriers

- PR #57's expanded worker suite exposed a two-phone fixture race: CI could let the guest pose expire between accepted start and fire, correctly producing `notRunning`. Both #57/#58 also exposed the same unchanged native calibration test using 12 ms sleeps against a 100 ms freshness budget. Native runtime sources were identical to parent #56 before this repair. Integration fixes both in #56 before propagating the parent to its children; no failed check is waived or rerun until it happens to pass.
- Worker fixture-only scheduling now crosses actual authenticated WebSocket/pong admission barriers, checks the exact pending command batch, and advances one real serialized authority tick. Simulation validation, SQL commit, durable sync and broadcasts remain intact. It asserts one accepted fire, identical two-client spawn data and the exact persisted spawn/result. A negative fixture still rejects fire when the other phone's pose expires. SocketInbox retains queued messages after close without sending invalid receipts. Production worker scheduling and freshness limits are unchanged; real-time load remains separate and its sustained failures stay recorded in the server slice.
- A subsequent #58 branch run exposed the same setup-time dependency in the older replacement-phone fixture. Replacement now proves fresh readiness/pose inputs were accepted before checking that a new socket clears them; refusal replay, identity conflict and recovery seeds also use controlled ticks. Four focused cases pass five independent runs. The deliberately faulted SQL commit retains its real timer/failure wrapper and gets a current authority timestamp after trigger installation. Parent canonical verification remains green at 239 tests; native sources are unchanged by this additional test repair.
- The native provider accepts a default-real clock and evaluates an observation with one consistent timestamp. Its 50 ms watchdog and 100 ms freshness limit remain unchanged. The test driver acknowledges processing when the provider requests the next event, replacing fixed sleeps; a controlled clock verifies exactly three fresh samples, immediate invisible-reference revocation, 101 ms stale rejection/recovery and firing-time expiry. Async teardown releases test streams even after an early thrown error.
- Verification: parent `pnpm verify` PASS (239 tests: 12 protocol, 39 simulation, 118 Convex, 40 workerd, 30 spectator). The server slice's 46 worker tests PASS, and three focused worker regressions pass in five independent runs. Native SwiftPM 281 tests PASS; four provider regressions pass in ten independent runs. Full local iOS gate passes shared simulation 74, transport 79 and native 281 tests plus unsigned Debug app/test and Release app builds. Final reference tests also pass after cleanup review. No physical-device, signing or deployment claim is made.

### 2026-09-05 — Four-player arena spectator and authority-owned expiry

- Integration accepted slice 008 from PR #56 head `2c10a9a` on separate branch `codex/arena-spectator`. Write ownership: spectator/design slice to the UI owner; `convex/domain/snapshot.ts` and its tests to the backend owner; this evidence record and final review to integration. This is a sibling of the server performance work and depends on #56's existing optional arena projection fields.
- The scoreboard retains all two to four players in server order, shows capacity, K/D and authoritative winner/draw, and distinguishes calibration, running, paused and unavailable authority state. Arena matches no longer appear finished solely because a legacy estimated wall deadline elapsed; classic expiry is unchanged.
- Connection loss retains the last snapshot and timestamp; recovery describes the viewer socket only and its announcement ends after 1.8 seconds even when fresh snapshots arrive. Independent review repaired a cached countdown rewind, StrictMode's disposed memoized adapter, and a screen-reader label that still promised respawn after finish. Lazy socket acquisition/last-subscriber release is covered through the actual App. Retained data keeps normal text contrast during loss.
- Local canonical `pnpm verify` PASS: 263 tests (12 protocol, 39 simulation, 125 Convex, 38 workerd, 49 spectator), lint/typecheck/build/repository gates and Worker dry run. No native source or shared wire contract changed. The inherited PR #56 native CI evidence remains separate.
- Eight [actual React browser fixtures](../spectator/evidence/008-arena-spectator/README.md) cover desktop, 375 px, long names, 200% text, calibration, pause, results and connection interruption/recovery, with no measured page/card overflow. These are synthetic local browser evidence, not production network, screen-reader or physical-iPhone acceptance.
- Remaining: live Convex disconnect/reconnect exercise, supported-browser keyboard/screen-reader trial, authority projection receipt/sequence contract, full terminal-ledger publication and spatial worldlines. No deployment, signing, physical-device, authority-freshness or full M0–M6 completion is claimed. Review order: #53 → #55 → #56 → this sibling slice.
- After inheriting parent `7cff8c1`, canonical `pnpm verify` passes 265 tests (including 40 worker and 49 spectator tests). Integration retained both evidence entries when resolving the append-only build-log conflict. Spectator code and browser evidence are unchanged by the shared parent repair; PR #58 CI runs again on the propagated head.

### 2026-09-05 — Durable combat storage performance and sustained-load failure

- Integration owns `codex/combat-runtime-performance`, based on PR #56 head `2c10a9a`; this is a sibling of the four-player spectator slice. Write set: worker runtime/tests/benchmark configuration, dependency lock and integration evidence. A separate runtime owner changed only SQL command/projection lookups and their actual workerd cursor tests; independent reviewers checked replay compatibility, byte accounting and benchmark acceptance.
- New-command lookup reads zero history rows instead of 513 at full retention; oldest replay reads two. A partial projection index reads one row instead of scanning 2,048 sealed retries. Compact canonical SHA-256 identities preserve exact retries of old canonical-JSON rows and reduce retained input payloads. Broadcast encoding is shared while each socket retains independent byte/receipt limits. Durable commit-before-output and tracking/authority limits remain unchanged.
- Canonical `pnpm verify` PASS: 243 tests (12 protocol, 39 simulation, 118 Convex, 44 workerd, 30 spectator), lint/typecheck/build/repository gates and Worker dry run. No native or shared-wire source changes. The new Node type dependency is pinned; its peer-resolution lock changes were reviewed.
- Two 30-second local four-player maximum-payload scenarios passed after optimization. Miss-lane allocation fell from 23,416,832 to 2,076,672 bytes; 352 accepted shots matched all four durable ledgers with zero cancellation, versus 88 cancelled bullets in the preserved failed baseline. Baseline/optimized short drivers both awaited clock replies in the pose loop, which is an explicit measurement limitation.
- Independent review then separated clock refresh from pose scheduling and added per-shot identity reconciliation. **Both three-minute scenarios failed smooth-play acceptance:** 724 of 2,048 miss-lane shots and 20 of 652 opposing-combat shots were cancelled during tracking pauses. Every accepted shot retained exactly one durable spawn and terminal; all four complete ledgers agreed with zero missing/duplicate/unresolved events and no authority epoch reset. Allocations were 4,759,552 and 2,813,952 bytes. [Raw results, provenance and reproduction](research/backend-evidence/runtime-load/README.md) retain all failures and distinguish scheduling/clock uncertainty from actual server CPU measurements.
- No production authority selection, deployment, physical-device or latency guarantee is claimed. Next work: isolate sustained scheduling/simulation/storage cost; bound repeated-epoch outbox growth without losing accepted terminals; persist retry scheduling and improve idle delivery; run identical host/DO and actual Wi-Fi/LTE/device scenarios. Stable-epoch retries already coalesce and do not alone imply unbounded growth. Compact rows require a digest-compatible rollback target for active rooms. Review order remains #53 → #55 → #56 → this sibling slice; full M0–M6 acceptance stays open.
- After inheriting parent `7cff8c1`, canonical `pnpm verify` passes 245 tests, including 46 worker tests. The shared helper conflict was resolved to the verified deterministic parent fixture and both integration log entries were retained. Runtime benchmark source is unchanged; captured helper hashes identify the historical load fixture, before the later test-only scheduler/cleanup repair. PR #57 CI runs again on the propagated head.

### 2026-09-05 — Profile collision cost and reuse bounded per-tick geometry

- Parent/game/spectator integration is green on GitHub at #55 `8c96fad`, #56 `73f0edf`, #57 `483e3e8` and #58 `77f2663`, each with both Fast and iOS gates passed. The following work extends the server performance draft. Integration explicitly owns the shared simulation change, delegates only `flight.ts` to the collision owner, and owns its regression tests, worker benchmark instrumentation and evidence. No native, wire or gameplay rule changes.
- Benchmark-only method observers retain fixed duration histograms and up to 64 coverage-pause records; client diagnostics retain bounded clock/capture/refusal histories. Independent review confirms original methods/promise identities and teardown are preserved. Timings are inclusive local wall time; both probes advanced at 1 ms resolution. Synthetic receive-side clock anchoring differs from the native estimator and remains an explicit limitation.
- The instrumented baseline reproduces cancellation (75 miss-lane / 22 opposing shots). Miss-lane simulation advance averages 22.94 ms and peaks at 89 ms, while store commit averages 0.71 ms. Clock adjustments remain within ±1 ms in this sample. The resolver repeatedly selects/interpolates the same tracked body for multiple bullets.
- The resolver now reuses exact target/time-interval geometry within one collision-candidate pass, up to 128 entries, with uncached overflow and lazy shield poses. Sweep code reads those values; the unchanged global impact loop mutates health/shields only after candidate collection. No history, epoch, stale-pose rule or target choice survives in a later cache. Four regressions cover 128-shot output equivalence and history-read reduction, distinct historical/current intervals, later-call invalidation, and repeated overflow computation with preserved hits. Independent review strengthened the overflow test.
- A controlled repeat changes only `flight.ts` in the source-hash manifest. Both profiled 30-second scenarios pass with zero cancellation and exact accepted-shot/spawn/terminal/4-client-ledger agreement. Miss-lane advance averages 6.54 ms (about 71% lower), with max 35 ms; opposing advance averages 3.77 ms. These are coarse local observations, not cloud CPU or a statistical latency guarantee.
- **Sustained acceptance remains incomplete.** With profiling disabled, three-minute opposing combat passes (584 accepted/durable shots, zero cancellation). Miss lanes fail with 120 cancelled out of 2,096 accepted/durable shots and 242 ms paused time. Both retain all accepted spawn/terminal identities, all four exact ledgers, no unresolved/gap/healed/duplicate events and authority epoch one. Pose send p99 is 52 ms and no pump slots are missed; occasional stale admission/coverage still needs independent-process, native-clock and queue/catch-up diagnosis. [Raw before/after/long reports](research/backend-evidence/runtime-load/README.md) preserve failures and source provenance.
- Final canonical `pnpm verify` PASS: 249 tests (12 protocol, 43 simulation, 118 Convex, 46 workerd, 30 spectator), lint/typecheck/build/repository gates and Worker dry run. No physical-device, signed install, live deployment, authority selection or complete M0–M6 acceptance claim is made.

### 2026-09-06 — PR #58 integrates the merged backend performance parent

- Integration merges parent `codex/realtime-combat-foundation` at `1bcc81a` (the merged PR #57) into spectator PR #58. The only conflict is this append-only evidence log; both the spectator and backend performance records are retained in full. Exclusive conflict write set: `docs/build-log.md`; all other incoming files match the parent without manual changes.
- Independent review confirms spectator UI, Convex projection behavior, slice 008 and browser evidence are byte-for-byte unchanged from PR #58 head `77f2663`; runtime and shared simulation files exactly match parent `1bcc81a`.
- Canonical `pnpm verify` PASS: 275 tests (12 protocol, 43 simulation, 125 Convex, 46 workerd and 49 spectator), lint/typecheck/build/repository gates and Worker dry run. The isolated worktree's locked dependencies were refreshed before verification. No deployment, signing or physical-device acceptance is implied by conflict resolution; GitHub checks run again on the published merge commit.
- Delivery reconciliation: #55 was squashed into #53, #56 into the former #55 branch, and #57 into the former #56 branch. Main at `08d9151` therefore has exactly the tree of `8c96fad`, an existing ancestor of this branch; the later game/backend changes have not reached main. Integration records main as a merge parent without changing the verified content, and retargets #58 to main as the final game/backend/spectator integration PR. Its title and description reflect this full delivery scope. No code or previously recorded evidence is dropped.

### 2026-09-06 — Visible incoming fire and compact in-match controls

- The user's TestFlight screenshot identifies the classic duel: the bottom control card covers approximately two-fifths of the camera view. It is replaced with a compact ammo/reload/fire row and a health/time/menu row. Scores, voice, help and leave move into a scrollable menu; empty toast containers no longer reserve height. The debug fallback button is removed from both the game and menu as requested, preserving the internal debug API. Arena receives equivalent compact controls while shield and slow-field actions remain directly available.
- Code inspection confirms that classic incoming shots were silently visual-free when no fresh observed head supplied an origin. The renderer now uses a camera-relative cosmetic cue in that case, a thicker orange streak and a bounded 180–300 ms travel presentation. It does not change hit registration, damage or the authoritative projectile rules. Explicit `renderTracer` control preserves peer/durable dedup and keeps arena confirmed-hit haptics from adding a second cosmetic ray to its finite flight.
- Menu opening cancels held fire and classic voice recognition, while tracking and heartbeat continue. Direct touch, voice and accessibility single-fire entry points reject firing through an open menu; closing it does not resume held fire. Existing cooldown, ammo, reload, tracking and network authority remain unchanged.
- Write ownership: root integration owns laser policy/renderer, existing presentation tests, preview harness and evidence; the UI agent owns the two view files and slice 009 until handoff. Independent code review identified an iOS-only name-shadowing error, corrected before successful iOS compilation. Independent layout review passes the captured resting classic HUD at 375×667 and 393×852.
- `pnpm verify` PASS: 275 workspace tests, lint, type checks, builds, repository checks and Worker dry run. `pnpm verify:ios` PASS: 74 shared simulation, 79 transport and 283 native app tests, plus unsigned simulator Debug app/test-target and Release app builds. Two new policy regressions cover absent/invalid observed origins, camera rotations, visible bounded lifetime and duplicate-cue suppression; existing incoming batch/reconciliation tests pass.
- [Four actual SwiftUI layout captures and reproduction](../design/evidence/009-gameplay-visibility/README.md) are macOS renders, not iPhone screenshots. Enlarged iOS text, safe areas, menu interaction, physical camera/laser visibility and signed TestFlight installation remain unverified. This update has not been installed on the user's phone. Separate unpublished backend load-driver work is excluded.

### 2026-09-06 — Repair the arena-creation deployment blocker

- A physical-phone report of “Unable to Continue / CAN’T REACH THE DUEL” occurred when creating an arena. The app sends `combatMode` and `maxPlayers` to `matches:create`; those validators were added in PR #58. The generic SDK-error mapping also labels server validation failures as network failures, so the alert alone does not diagnose phone connectivity.
- Both main deployment runs `34064608587` and `34065412395` failed while their TestFlight promotions succeeded. The latter Convex deployment reports `InvalidConfig`: `lib/combat-projection.js` contains a forbidden hyphen. This prevents the newer backend from reaching production and supports the app/backend version mismatch diagnosis. Arena creation itself does not contact the combat worker.
- Isolated repair ownership: the debugging agent renames the two deployed helper modules to `combat_projection.ts` / `combat_ticket.ts` and updates their imports under `convex/**`; integration owns the repository path preflight, this evidence record and delivery. Helper contents are byte-identical. No combat timing, native behavior, signing or manual deployment is changed.
- The new repository preflight checks Convex source path components before CI can approve an undeployable revision. A negative fixture rejects the original hyphenated filename and an invalid directory; the underscore repair passes. All ten deployed source paths pass. Canonical `pnpm verify` PASS: 275 tests, lint, type checks, builds, repository checks and Worker dry run (`/tmp/vkz-arena-creation-verify.log`). Reviewed production deployment remains required; no successful phone retry is claimed yet.

### 2026-09-06 — Require deployed backend evidence before TestFlight

- PR #61 repairs the Convex module paths that blocked arena creation. Investigation also confirmed a release ordering gap: Deploy and TestFlight independently followed CI, allowing a signed app to reach TestFlight while its backend deployment failed. This follow-up enforces the existing delivery contract; it does not alter gameplay or deploy a combat authority.
- TestFlight now follows successful Deploy completion. The gate verifies current main, canonical main-push CI, candidate-attributed deployment runs, the current run attempt's successful deployment/smoke steps and nonexpired SHA-named evidence created during that attempt. Manual dispatch uses the same prerequisites. The Outpost rechecks them before archiving, so queued work cannot rely on stale hosted-gate approval.
- Deploy's run name carries its actual checkout SHA before any step can fail, avoiding chained workflow `head_sha` ambiguity. History is bounded to the candidate's first canonical CI creation time and matched to that SHA; unrelated earlier releases cannot block it. Incomplete/truncated candidate-era history fails closed. Partial retries reusing earlier-attempt jobs or artifacts do not qualify; a fresh full Deploy is the supported recovery.
- The release agent owns the two workflows and release helpers/tests through handoff. Integration reviewed real GitHub job metadata and the timestamp filter, corrected lifetime-history scanning, and owns final verification plus `scripts/ci/verify.sh`. Both pure Node release self-tests now run inside `pnpm verify`, preserving their coverage in CI without building fixture URLs into the final spectator output.
- Final `pnpm verify` PASS: 275 workspace tests, both release self-test suites, lint, type checks, builds, module-path preflight and repository checks (`/tmp/vkz-release-gate-verify.log`). Both workflow YAML parses and diff checks PASS. Cases cover failed/pending/skipped deployments, missing/wrong/expired evidence, reruns, pagination, unrelated releases, manual dispatch and prearchive failures. The Outpost runbook documents the deployment prerequisite and full-rerun recovery. Reviewed live promotion remains unverified; no signing, deploy, TestFlight upload or phone installation is performed by this change.

### 2026-09-06 — Server-arrival scheduling and independent sustained-load handoff

- The backend owner completed the bounded `services/combat-worker/**` and runtime-load evidence checkpoint, then released ownership to integration. Integration reviewed scheduling, startup cleanup and the shared workload. The gameplay fixes in merged PR #60 and the arena-creation deployment repair in PR #61 are separate changes.
- Controlled authenticated handler injection proved that a delayed catch-up tick consumed twelve queued poses at logical time 150 ms, rejected eight valid later captures as future input, then paused coverage and cancelled an existing projectile. Assigning ticks from trusted monotonic handler-entry time admits the sets at 150/200/250 ms and preserves running flight. A reviewed reconnect guard retains cadence while deferred commands remain, preserving sequence order. Clock, timestamp, epoch, fire/ammo and queue limits remain unchanged.
- The independent Node driver runs the unchanged four-player workload against a separate workerd process, uses the native clock estimator and per-client heartbeats, closes late or failed startup resources, and marks interrupted runs as partial and unreconciled. The four clients share one Node event loop. No phone, cloud route, authority-selection, projection delivery or production performance claim is made.
- Final canonical `pnpm verify` on the integrated delivery tree PASS: 314 tests (12 protocol, 43 simulation, 125 Convex, 85 worker/driver, 49 spectator), lint/types/build/repository checks and Worker dry run (`/tmp/vkz-backend-delivery-verify.log`). This includes the final deterministic negative-control clock correction and the reconnect guard. All 12 focused cases also pass. After PR #61 merged, integration preserved both build-log records and repeated the full 314-test gate with its corrected Convex paths and new module preflight (`/tmp/vkz-backend-merged-verify.log`); PASS.
- Both unchanged 30-second scenarios completed and reconciled four exact ledgers and accepted/spawn/terminal identities, but **failed** zero cancellation: 32 of 352 miss-lane shots and 10 of 170 opposing shots were cancelled. All clients had 600 accepted poses and no pose refusals. The requested three-minute scenarios **failed early** at 80,678 / 27,381 ms with clock-quality loss / recovery and 64 / 12 observed cancellations; neither completed reconciliation. Runtime/benchmark source manifests match across these attempts. No retries or tuned acceptance thresholds conceal the failures.
- [The backend handoff](research/backend-handoff.md) and [raw runtime evidence](research/backend-evidence/runtime-load/README.md) preserve the startup failures, original before/after diagnostics and sustained failures. Next evidence must correlate live handler arrival, queue residence, processed tick and coverage age around the first pause before another timing repair. Full M5/M6 and physical-device acceptance remain open.

### 2026-09-06 — Bounded first-pause trace and live-configuration handoff

- Integration delegated benchmark instrumentation, its focused test and runtime-load evidence in `/tmp/vkz-combat-coverage-trace`; it later delegated this entry, the roadmap and backend handoff. The trace uses a separate benchmark Worker subclass/configuration, observes production handler-entry time, queue residence, admission, assigned/consumed ticks and exact failed coverage, and retains at most 64 inputs, eight ticks and one 32,768-byte log record. It does not change production runtime, shared contracts, clocks or acceptance thresholds.
- The one serial 30-second pair passed: 352 miss-lane and 136 opposing accepted/durable shots, zero cancellations, four exact ledgers, matching accepted/spawn/terminal identities and zero unresolved bullets. Both observers were armed with zero malformed records. [The raw report](research/backend-evidence/runtime-load/node-first-pause-30s.json) preserves measured HEAD `7a1a529` and exact source hashes; the later integration checkout is main `541c06e01d530ca36d60d620454a953616dff082`.
- Opposing combat nevertheless paused at the first post-arm tick, 47 (2,350 ms), before the first measured pose send, then restored at tick 48: about 51 ms observed paused. Accepted poses were 119.7–120.3 ms old; four newer poses were pending for tick 48, with zero eligible for tick 47, 0 ms queue residence at available resolution and 55 ms cadence lag. The roughly 117 ms capture gap may include harness environment/DO-proxy initialization on the shared Node loop. This arming-boundary confound is retained; the report does not establish the cause of prior sustained cancellations or three-minute failures. No further cache change, benchmark retry or runtime repair was made.
- Five observer regressions and existing startup/scenario lifecycle tests pass (20 focused), plus worker typecheck, lint and diff checks. Tests cover original simulation/promise behavior, rejected durable commit, unarmed warmup, bounded input/tick/log retention and exclusion of unrelated harness logs. The four synthetic clients share one Node event loop and pose pump; workerd is a separate process. Local timing and ledger evidence do not establish cloud/phone latency, live projection or full M0–M6 acceptance.
- Release status supplied by integration: PR #64 merged at main `541c06e01d530ca36d60d620454a953616dff082`. Draft PR #65 head `9f74e8716cd2999e62792108154a78094921a5c5` adds guarded Worker deployment and has not deployed it. The phone now reaches Align Arena but reports live combat is not configured. An authenticated Convex dashboard confirmed no environment variables; the current production deploy key separately lacks `deployment:env:view`. Private keys are prepared and user paste is pending. The diagnostic agent inspected no secret files and performed no external writes, configuration changes, Git mutations or deployments; root integration owns publication of this checkpoint.
- Canonical `pnpm verify` ran once on this trace tree based on main `541c06e01d530ca36d60d620454a953616dff082`: **PASS, 319 tests** (12 protocol, 43 simulation, 125 Convex, 90 worker/driver, 49 spectator), both release self-test suites, lint, types, builds, repository/module-path checks and Worker dry run. Log: `/tmp/vkz-coverage-trace-verify.log`; session `59128` terminated with exit 0. All 45 measured source/configuration hashes still match the preserved report. This gate does not verify the separate draft PR #65. No benchmarks were rerun; all diagnostic processes are terminal and paths are released to integration.

### 2026-09-06 — Guard the missing live-combat deployment

- After the arena-creation repair deployed, the owner confirmed that creating an arena now reaches Align Arena, which reports “LIVE COMBAT IS NOT CONFIGURED”. The production dashboard showed no environment variables, and authenticated Cloudflare lookup returned Worker-not-found for `vkz-combat`. This is separate from the repaired Convex module-name failure.
- Integration adds a guarded Worker bootstrap and operator runbook on `codex/live-combat-release`. The release owner supplies the script and focused self-tests; integration owns canonical CI wiring and this record. It depends on merged PR #64's same-SHA deployment gate. The current Convex deploy key lacks environment-view permission; no permission was expanded or credential value exposed.
- The script requires a clean current-main checkout, canonical CI and deployed/smoked Convex/spectator evidence, an explicitly selected authorized Cloudflare OAuth account, two independently supplied keys and the exact configured origins. It dry-runs, rechecks release state, then deploys both secrets atomically and checks the structured version receipt and limited health response. The pure Node regression suite runs in canonical CI and performs no cloud operations.
- Browser credentials/settings require operator handoff. A private local import file contains two independent generated keys and the intended Worker origin; no secrets enter Git, logs, commands or chat. Until the operator confirms those settings and the reviewed script completes, Align Arena remains unverified. Health alone cannot prove matching keys, authenticated WebSocket admission, projection delivery, calibration or physical combat acceptance.
- Canonical `pnpm verify` PASS on the integrated bootstrap tree: 314 workspace tests, all three release self-test suites, lint, types, builds, module preflight and repository checks (`/tmp/vkz-live-combat-verify.log`). Independent review confirmed pinned Wrangler OAuth/receipt schema compatibility. No Worker deployment or authenticated phone acceptance is claimed by these checks.
- PR #65 review identified a stale-release window during the final Cloudflare identity lookup. A deterministic regression reproduced an actual deployment call after the simulated main branch advanced (`/tmp/vkz-combat-final-gate-red.log`). Integration moved the final release gate after identity validation and the current-main lookup after CI/Deploy evidence reads. GitHub blocked the attempted merge on its unresolved review thread; no merge or Worker deployment occurred during this repair.
- The regression now rejects the changed release before deployment or evidence writing. All three focused release self-test suites pass. Canonical `pnpm verify` PASS on the repaired tree: 314 workspace tests and all existing gates (`/tmp/vkz-live-combat-review-fix-verify-unsandboxed.log`, process exit 0). The initial sandbox attempt stopped with `EPERM` for a localhost listener and Wrangler's log directory; verification was rerun with permission for those local operations. No failing gameplay assertion was bypassed or acceptance limit changed.


### 2026-09-07 — Repair native combat connection bootstrap ordering

- After the combat Worker was configured and deployed, the owner reported a stuck “Connection needs attention” screen and missing departure updates. The Worker inserted each admitted socket before broadcasting its player change, so an iPhone received an event batch before its initial snapshot. The native replica correctly refused an event without a baseline; repeated connection attempts produced the generic Wi-Fi/mobile-data guidance. The earlier short Node smoke accepted that invalid ordering and did not prove native connection readiness.
- A stricter [production probe](research/backend-evidence/socket-baseline/pre-fix.json) reproduces `first-message-not-snapshot` against deployed commit `17e4a50771dd7caa59b915aa9acc6c0ccbac5280`. Three setup stages pass, then host admission fails. It creates one synthetic match and two players, closes its socket and retains the synthetic rows; no shots, calibration or physical acceptance is claimed.
- Integration changes only Worker message ordering: send the committed baseline before admission events, and send a recovered authority baseline before the epoch hint and new-epoch events. Existing peers retain their admission/replacement updates. Authentication, durable commit ordering, clock limits and combat rules remain unchanged. Four runtime regressions fail before the repair and pass afterward, covering first admission, joining an existing room, socket replacement and stalled-authority recovery.
- The separate lobby-departure investigation found no explicit Convex leave mutation or lobby presence expiry. This hotfix repairs the native combat connection and its ability to receive live updates; pre-alignment lobby removal remains a separate follow-up. Production deployment and real Swift transport verification of this fix remain pending at this checkpoint.
- Canonical `pnpm verify` PASS on the integrated hotfix: 323 workspace tests, all three release self-test suites, lint, type checks, builds, repository/module checks and Worker dry run (`/tmp/vkz-snapshot-first-verify.log`, process exit 0). The focused admission/runtime suite passed 22 tests after four ordering regressions failed against the original code. No failed assertion or clock threshold was bypassed.

### 2026-09-06 — Configure production combat and record the initial connectivity smoke

- Integration records the production Worker bootstrap from then-current main `17e4a50771dd7caa59b915aa9acc6c0ccbac5280`. The existing deployment key denied environment reads and writes. The owner approved login through the official Convex CLI; `COMBAT_TICKET_SECRET`, `COMBAT_PROJECTION_SECRET` and `COMBAT_WORKER_URL` were then saved, and exact readback parity with the prepared values was verified without exposing them. This documentation handoff writes only this log and sanitized launch evidence.
- The guarded same-SHA preflight passed and the structured publish receipt validated Worker version `28d4691e-ad85-4b43-8ab6-90f725f8c3bf`. Its first health check failed, so bootstrap exited 1. Subsequent read-only recovery checks found HTTP 200, protocol 1, projection configured and one active version receiving 100% of traffic; no redeployment occurred. The [deployment receipt](research/backend-evidence/live-combat-launch/deployment.json) preserves the initial failure and the limited recovery evidence.
- A [separate synthetic production smoke](research/backend-evidence/live-combat-launch/connectivity.json) passed all eight stages, including arena preparation, both authenticated WebSocket connections, both rosters and observed Convex guest-presence transitions from disconnected to connected to disconnected. Both sockets closed and acknowledgements covered only received sequences. One synthetic match and two player rows remain because no public deletion/end/leave API exists. No shots or calibration commands were sent; these observations do not expose an exact outbox receipt cursor or prove physical-device acceptance. The Node client accepted event batches before its initial snapshot, so this passing smoke did not validate native message ordering or playable connection readiness.
- Same-SHA [CI 34072174255](https://github.com/Jorybraun/victoria-kill-zone/actions/runs/34072174255) passed canonical workspace and iOS checks. [Deploy 34072502857](https://github.com/Jorybraun/victoria-kill-zone/actions/runs/34072502857) passed actual Convex/Pages deployment and both smoke checks, corroborated by its sanitized receipt. [TestFlight 34075301520](https://github.com/Jorybraun/victoria-kill-zone/actions/runs/34075301520) uploaded version 0.1.1 (38); its receipt reports ready-for-testing and Apple processing VALID. Installation and gameplay on the owner's phone remain unverified; full M0–M6 acceptance stays open.
- Canonical `pnpm verify` PASS on the original PR #67 head `8a714a7`: 319 workspace tests (12 protocol, 43 simulation, 125 Convex, 90 Worker and 49 spectator), all three release self-test suites, lint, types, builds, module/repository checks and Worker dry run. Frozen offline installation reused the existing local dependency store; no dependency or lockfile changed. Sanitized local log: `/tmp/vkz-live-combat-launch-verify.log`; process exit 0.

- Subsequent [actual Swift transport evidence on macOS](research/backend-evidence/live-combat-launch/native-pre-fix.json) failed for both host and guest against the original deployed SHA: the first message was `events`, both replicas rejected it with `invalidSnapshot`, and neither accepted a baseline or reached clock readiness. This compiled the active Swift decoder, replica, clock and URLSession transport; it is not a physical-iPhone trial. Its separate synthetic match and two player rows remain after socket closure. The stricter Node failure is preserved in [PR #68's probe](research/backend-evidence/socket-baseline/pre-fix.json). PR #68 merged the snapshot-ordering repair at `2929fd69e6768192f91fbf206cbed62cb202626e`; deployment and post-fix live validation of that revision were pending at that checkpoint.


### 2026-09-07 — Deploy the snapshot-ordering repair and validate the native transport

- Exact main `2929fd69e6768192f91fbf206cbed62cb202626e` passed [CI 34080242074](https://github.com/Jorybraun/victoria-kill-zone/actions/runs/34080242074) and [Deploy 34080570283](https://github.com/Jorybraun/victoria-kill-zone/actions/runs/34080570283). The guarded preflight and Worker deployment then passed, publishing version `0eca600a-c9ae-4262-b4c2-eb0f5d2bf0b6`. The [new deployment receipt](research/backend-evidence/live-combat-launch/snapshot-first-deployment.json) confirms the release identity and configured health response; its deliberately limited acceptance fields remain unchanged. A subsequent independent read-only status check confirms that this version is the sole active version receiving 100% of traffic, with HTTP 200 and projection configured; historical deployment entries are not counted as active versions.
- The [actual Swift transport probe](research/backend-evidence/live-combat-launch/native-post-fix.json) now passes against that deployed revision. Both clients receive a snapshot first, accept the baseline, reach clock readiness and complete their bounded holds with no ready-to-unready disconnect condition. After the host closes, the guest applies one fresh peer-disconnect event and its final replica marks the peer disconnected. The same four active Swift source files used by the earlier failing probe are byte-for-byte unchanged in the new commit, verified against the receipt's SHA-256 manifest. These are macOS URLSession/decoder/replica/clock observations, not physical-iPhone or AR/fire evidence.
- This native probe creates one separate synthetic match and two player rows, closes its sockets and retains those rows. The user's phone retry remains pending. Pre-alignment lobby removal, calibration, poses, shots, sustained load and full M0–M6 acceptance are not established by these connectivity checks.
- Canonical `pnpm verify` PASS after rebasing the documentation branch onto the repair: 323 workspace tests (12 protocol, 43 simulation, 125 Convex, 94 Worker and 49 spectator), all three release self-test suites, lint, types, builds, module/repository checks and Worker dry run. Sanitized local log: `/tmp/vkz-live-combat-launch-rebased-verify.log`; process exit 0. Subsequent changes add evidence documentation only.

- The separate [strict Node production probe](research/backend-evidence/live-combat-launch/stability-post-fix.json) passes all nine stages, completes its full 30-second hold and finishes in 34.181 seconds. Both sockets receive a snapshot first and their native-formula clocks become ready and remain ready through the last observation. Host/guest send 37/35 pings and receive 132/124 snapshots, with zero recorded readiness losses, transport errors, receipt anomalies or pong anomalies. Convex guest presence is observed disconnected → connected → disconnected. Both sockets close; this probe's additional synthetic match and two player rows remain. This bounded idle transport/clock sample is not a sustained combat or physical-device trial.

### 2026-09-07 — Recover host scanning and unarmed calibration clock synchronization

- After the snapshot-ordering server repair was deployed at `2929fd69e6768192f91fbf206cbed62cb202626e`, the owner reported that the phone creating the arena remained scanning and sometimes reconnected. Its exact ARKit mapping state and device model have not been captured; this is a user-reported physical-device failure, not a reproduced device acceptance result.
- Inspection found that three camera paths required exactly ARKit `.mapped`, although Apple's [world-map saving sample](https://developer.apple.com/documentation/arkit/saving-and-loading-world-data) accepts `.extending` as well. One shared predicate now permits either usable status with normal tracking for the scan-ready observation, reference capture and map capture. This only permits map capture; both relocalization stages and measured alignment still gate firing.
- Host scanning now has a 30-second deadline when no usable map arrives, including missing camera callbacks. Becoming ready clears that deadline while the host chooses a reference; losing map readiness starts a fresh bounded scan. Joining phones can continue waiting for the host, while their existing 15-second installation deadlines remain enforced. The host gets explicit recovery guidance and a Restart scan action.
- A ready clock becoming stale or uncertain previously closed even an unarmed calibration socket. A five-second recovery window now retains the socket only while the authority reports calibration with the local player connected and unready, with no unresolved commands or active writer. Old clock samples and ping nonces are discarded; fresh samples resynchronize timing while spatial submissions remain locked. Armed play, unresolved commands, authority changes, failed recovery and transport errors retain disconnect behavior. The 25 ms uncertainty and three-second freshness limits are unchanged, and submission now checks freshness at the instant of use.
- Integration owns `Features/Realtime/RealtimeArenaView.swift`, `RealtimeMapCoordinator.swift`, this log and release coordination; targeting owns the affected targeting source files and `DuelFramePolicyTests.swift`; the connection lane owns `RealtimeCombatSession.swift` and its existing test file. No wire protocol, backend, Xcode membership or dependency lockfile change is included. A new TestFlight build and a host-phone scan/reconnect trial are required before claiming this app repair works on device.
- Canonical `pnpm verify` PASS: 323 workspace tests and all release, type, lint, build and repository gates (`/tmp/vkz-arena-scan-verify.log`). `pnpm verify:ios` PASS under Xcode 26.6: the Swift packages, all 295 app tests, Debug app/test-target builds and the Release app build (`/tmp/vkz-arena-scan-ios-verify.log`). Portable actual-source regressions also preserved failing-before/passing-after evidence: 18 mapping/provider tests passed and six clock cases passed, including the armed-disconnect control. Independent source review found no actionable regression.
- Focused iPhone 17 Pro simulator execution on iOS 26.5 passed all 39 tests with zero failures or skips, including the ARKit-only extending-map case. The first attempt could not write its result bundle because the Mac disk was full; its log is preserved at `/tmp/vkz-scan-simulator.log`. Only attributable generated build outputs were removed, then the rerun's result bundle was read and its totals recorded at `/tmp/vkz-scan-simulator-summary.json`. No physical-device result is claimed.
- Initial GitHub CI run `34082884019` failed a new armed/pending calibration regression that had passed locally. A controlled counterexample showed that an already-queued pong could refresh the test's clock after its artificial time jump, which was then frozen forever. This is a fixture ordering error: the actual clock correctly accepts that sample. The failed CI log is preserved at `/tmp/vkz-arena-scan-ci-failure.log`; production timing thresholds are not weakened to resolve it.
- The corrected fixture disables automatic replies and waits for a FIFO snapshot-revision barrier before every artificial clock jump; pending arming commands are queued after that barrier. Ten consecutive focused runs passed all 60 cases with no failures (`/tmp/vkz-calibration-ci-repeat.log`), and canonical `pnpm verify` passed again (`/tmp/vkz-arena-scan-final-verify.log`). Only the test fixture and evidence record changed after the successful iOS builds and simulator run; no production timeout or clock threshold was increased.

- Conflict maintenance: merged main `03af9e8` into this PR after #67 added launch evidence. Both evidence sections and all six new receipt files are preserved; the iOS, backend, spectator, simulation, configuration and scripts trees are unchanged from this PR head. Canonical `pnpm verify` passed all 323 workspace tests and required gates (`/private/tmp/vkz-conflicts-69-verify.log`). This documentation-only merge does not claim a new native build, physical-device result or deployment.

### 2026-09-07 — Save arenas before creating a match

- Integration accepted `design/saved-arenas.md` for the owner's request to prepare a room ahead of a game and reuse its scan later. This slice depends on PR 69's scan recovery. `CREATE ARENA` now opens a library; `SAVED ARENAS` offers standalone scanning and local management. Setup captures a fixed natural reference, names and saves the bundle, then awaits camera teardown before returning. No lobby is created until the user explicitly chooses a validated saved arena.
- `LocalSavedArenaStore` owns Application Support storage, with generated filenames, atomic directory publication, bounded metadata, at most 12 maps of 8 MiB each, private permissions, backup exclusion and requested Complete file protection. Load rechecks size, hash, bundle/reference and secure ARWorldMap decoding. Damaged entries remain deletable. Saved metadata excludes epochs, poses, residuals, readiness, player/session identity and credentials.
- A created host session holds its immutable selection; join/leave clear it. After the authenticated first combat snapshot initializes the room, the map coordinator shares those bytes through the existing host-only map endpoint under the current match epoch. Identical published bytes are reused; conflicting bytes cannot overwrite a match's map. All phones still require fresh world and body relocalization plus measured reference residuals. No backend schema, Worker deployment, wire protocol or player-cap change is included.
- Exclusive ownership: storage agent authored the new models/store/storage tests; setup agent authored the new offline controller/view/setup tests; integration owns library/navigation/lobby/realtime composition, the map-transfer protocol, Xcode membership, integration tests and this record. Independent integration review found a classic-duel `onDisappear` camera stop that could run after offline setup began. LobbyStore now relinquishes its legacy camera ownership synchronously, shares one awaited teardown, and waits for pending camera start before stopping. A regression proves late classic cleanup cannot stop a subsequent offline camera owner; review confirmed the fix.
- Canonical `pnpm verify` passed twice, including all 323 workspace tests and release/lint/type/build/repository gates (`/tmp/vkz-saved-arenas-verify.log`, `/tmp/vkz-saved-arenas-final-verify.log`). `pnpm verify:ios` passed the Swift packages, initial 327 app tests and Debug app/test-target plus Release builds under Xcode 26.6 (`/tmp/vkz-saved-arenas-ios-verify.log`). After the ownership correction, all 328 app tests passed. The later storage-test refinement produced 329 app tests with zero failures and one explicit physical-device-only skip (`/tmp/vkz-saved-arenas-release-swift.log`).
- Simulator execution exposed a test assumption: Foundation's protection attribute is an NSString, not the Swift typed wrapper, and this iOS 26.5 simulator returned no protection readback even using the documented string representation. Tests now separate actual filesystem permissions/backup behavior, Complete protection requests crossing the real FileManager boundary, and strict physical-iOS attribute readback. The latter explicitly skips on simulator/Mac; no simulator encryption claim is made. A regular-file failure fixture also used a directory URL with a trailing slash and was corrected. Earlier failing logs and sanitized summaries are retained under `/tmp/vkz-saved-arenas-simulator*`.
- One simulator rerun could not write a complete result bundle after the Mac ran out of disk space; that run is not accepted as passing evidence. Only this task's completed generated build products were removed, preserving source, package checkouts, logs and result bundles. Final simulator and release-build results are recorded below when complete.
- Physical acceptance remains pending: scan and save on a named phone, restart the app and reuse the same room in a new match, prove both phones align and fire in the same frame, reject a moved/missing reference, and verify protected files on a locked physical device. This feature is not device-promoted or available in TestFlight yet. PR 69's separate merge/release awaits explicit user approval after automatic approval review rejected the protected-main merge.
- Final iPhone 17 Pro / iOS 26.5 simulator result bundle: **40 tests, 39 passed, zero failures, one physical-device-only skip** (`/tmp/vkz-saved-arenas-simulator-green.log`, `/tmp/vkz-saved-arenas-simulator-green-summary.json`). The FileManager boundary test was corrected after an isolated run showed that Foundation also makes two permissions-only calls during directory creation; it still requires four distinct Complete-protection requests and all expected modes. Actual on-device readback remains strict. The final Release app build passed (`/tmp/vkz-saved-arenas-final-release-build.log`); the final simulator run compiled the final Debug app and test target. The standalone UI has build coverage, but a visual/physical camera walkthrough is not claimed.
